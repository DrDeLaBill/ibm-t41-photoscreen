# .env — единый конфиг для setup/update/frame
# Используй абсолютные пути!

# Пользователь, под которым будут запускаться сервисы (не root)
RUN_USER="youruser"

# Базовая папка для всех видео (raw + converted)
BASE_DIR="/home/youruser/Videos"

# Куда сохранять временные/сырые загрузки (не трогать вручную)
RAW_DIR="$BASE_DIR/raw"

# Куда сохранять готовые (перекодированные) видео: плейлист-папки будут иметь суффикс _<SCREEN_HEIGHT>
# (напр., если RAW_DIR/PlaylistTitle/ — конвертируется в BASE_DIR/PlaylistTitle_480)
CONVERTED_BASE="$BASE_DIR"

# Файл архива для yt-dlp (чтобы не скачивать повторно)
YTDLP_ARCHIVE="$BASE_DIR/.archive.txt"

# Cookies (если используешь), если нет — оставь пустым
COOKIES_PATH="/home/youruser/cookies.txt"

# SOCKS/HTTP proxy (можно оставить пустым если напрямую выход в интернет)
PROXY="socks5h://127.0.0.1:1080"

# Плейлисты — разделять через вертикальную черту |
# Пример: PLAYLIST_URLS="https://youtube.com/playlist?list=PL1|https://youtube.com/playlist?list=PL2"
PLAYLIST_URLS="https://www.youtube.com/playlist?list=PLXXXXX1|https://www.youtube.com/playlist?list=PLXXXXX2"

# Желаемая высота видео (px) — все ролики будут перекодированы так, чтобы высота была эта величина
SCREEN_HEIGHT=480

# Частота (FPS) после перекодирования — 25 обычно стабильно
TARGET_FPS=25

# Путь для логов (yt-dlp и прочих)
LOG_DIR="$BASE_DIR/logs"

# Время запуска обновлений (systemd timer) в crontab-like формате:
# Мы используем systemd timer, но сохранить расписание здесь удобно:
# Пример: "03:15" — запуск в 03:15 утра
UPDATE_TIME="03:15"

# Опции ffmpeg (дополнительные) — при необходимости можно менять
FFMPEG_OPTS="-preset veryfast -crf 24 -pix_fmt yuv420p -profile:v baseline -level 3.0 -x264-params ref=1:bframes=0:cabac=0"

# yt-dlp дополнительные опции
YTDLP_OPTS="--no-part --no-mtime --ignore-errors --concurrent-fragments 1"

# Максимальное число попыток при скачивании одного видео
MAX_TRIES=5
